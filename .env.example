# Database
# SQLite database file location (relative to prisma folder)
# Default: file:./dev.db
DATABASE_URL="file:./dev.db"

# Node Environment
NODE_ENV="development"

# Next.js (automatically set, no need to change)
# NEXT_PUBLIC_ prefix makes variables accessible in browser
# Add your public environment variables here if needed
